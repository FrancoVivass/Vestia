import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Content-Type':'application/json'};
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});const url=Deno.env.get('SUPABASE_URL')!,serviceRole=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!,anon=Deno.env.get('SUPABASE_ANON_KEY')!;const admin=createClient(url,serviceRole);let createdUserId:string|undefined,createdBusinessId:string|undefined,uploadedLogoPath:string|undefined,stage='autenticación';try{const authorization=req.headers.get('Authorization');if(!authorization)throw new Error('No se recibió una sesión válida');const caller=createClient(url,anon,{global:{headers:{Authorization:authorization}}});const{data:{user},error:userError}=await caller.auth.getUser();if(userError||!user)throw new Error('La sesión expiró. Cerrá sesión y volvé a ingresar');const{data:platform,error:platformError}=await admin.from('platform_owners').select('active').eq('auth_user_id',user.id).eq('active',true).maybeSingle();if(platformError)throw platformError;if(!platform)throw new Error('Tu usuario no está habilitado como propietario de la plataforma');const body=await req.json();for(const key of['businessName','ownerFirstName','ownerLastName','email','password'])if(!String(body[key]??'').trim())throw new Error(`Falta completar ${key}`);const email=String(body.email).trim().toLowerCase();if(!/^\S+@\S+\.\S+$/.test(email))throw new Error('El email de acceso no es válido');if(String(body.password).length<8)throw new Error('La contraseña debe tener al menos 8 caracteres');stage='usuario de acceso';const{data:auth,error:authError}=await admin.auth.admin.createUser({email,password:String(body.password),email_confirm:true,user_metadata:{role:'OWNER',permissions:[],userStatus:'active',full_name:`${body.ownerFirstName} ${body.ownerLastName}`.trim()}});if(authError)throw new Error(authError.message.includes('already')?'Ya existe un usuario con ese email':authError.message);createdUserId=auth.user.id;stage='comercio';const{data:business,error:businessError}=await admin.from('businesses').insert({name:String(body.businessName).trim(),legal_name:String(body.legalName||body.businessName).trim(),tax_id:String(body.taxId??'').trim()||null,email:String(body.businessEmail||email).trim().toLowerCase(),phone:String(body.phone??'').trim()||null,address:String(body.address??'').trim()||null}).select('id,name').single();if(businessError)throw businessError;createdBusinessId=business.id;if(body.logoBase64){stage='logo';const mime=String(body.logoMime??'').toLowerCase(),extension=({'image/jpeg':'jpg','image/png':'png','image/webp':'webp','image/svg+xml':'svg'}as Record<string,string>)[mime];if(!extension)throw new Error('El logo debe ser JPG, PNG, WEBP o SVG');const bytes=Uint8Array.from(atob(String(body.logoBase64)),char=>char.charCodeAt(0));if(bytes.length>5*1024*1024)throw new Error('El logo no puede superar 5 MB');uploadedLogoPath=`${business.id}/business-logos/logo.${extension}`;const{error:uploadError}=await admin.storage.from('business-assets').upload(uploadedLogoPath,bytes,{contentType:mime,upsert:true});if(uploadError)throw uploadError;const{error:logoError}=await admin.from('businesses').update({logo_path:uploadedLogoPath}).eq('id',business.id);if(logoError)throw logoError;}stage='perfil OWNER';const{error:profileError}=await admin.from('profiles').insert({auth_user_id:createdUserId,business_id:business.id,first_name:String(body.ownerFirstName).trim(),last_name:String(body.ownerLastName).trim(),email,phone:String(body.ownerPhone??'').trim()||null,role:'OWNER',active:true});if(profileError)throw profileError;stage='configuración inicial';const methods=[['CASH','Efectivo'],['DEBIT','Débito'],['CREDIT','Crédito'],['TRANSFER','Transferencia'],['MERCADOPAGO','Mercado Pago'],['QR','QR'],['OTHER','Otro']];const operations=await Promise.all([admin.from('payment_methods').insert(methods.map(([code,name])=>({business_id:business.id,code,name}))),admin.from('cash_registers').insert({business_id:business.id,name:'Caja principal'}),admin.from('sizes').insert(['XS','S','M','L','XL','XXL','36','38','40','42','44','46'].map((name,index)=>({business_id:business.id,name,sort_order:index+1}))),admin.from('app_settings').insert({business_id:business.id,store_name:business.name,logo_path:uploadedLogoPath??null,email:body.businessEmail||email})]);const operationError=operations.find(result=>result.error)?.error;if(operationError)throw operationError;return new Response(JSON.stringify({businessId:business.id,businessName:business.name,userId:createdUserId,email,logoPath:uploadedLogoPath??null}),{headers:cors});}catch(error){if(uploadedLogoPath)await admin.storage.from('business-assets').remove([uploadedLogoPath]);if(createdBusinessId){for(const table of['app_settings','sizes','payment_methods','cash_registers','profiles'])await admin.from(table).delete().eq('business_id',createdBusinessId);await admin.from('businesses').delete().eq('id',createdBusinessId);}if(createdUserId)await admin.auth.admin.deleteUser(createdUserId);const message=error instanceof Error?error.message:'Error inesperado';return new Response(JSON.stringify({error:`Error en ${stage}: ${message}`}),{status:400,headers:cors});}});
