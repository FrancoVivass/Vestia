import{CurrencyPipe}from'@angular/common';import{Component,OnInit,inject,signal}from'@angular/core';import{FormsModule}from'@angular/forms';import{CashService,CashSummary}from'../../../../core/services/cash.service';import{DataAccessService}from'../../../../core/services/data-access.service';import{ToastService}from'../../../../core/services/toast.service';
@Component({selector:'app-cash-page',imports:[FormsModule,CurrencyPipe],templateUrl:'./cash-page.html',styleUrl:'./cash-page.css'})export class CashPageComponent implements OnInit{private readonly cash=inject(CashService);private readonly data=inject(DataAccessService);private readonly toast=inject(ToastService);readonly session=signal<any>(null);readonly summary=signal<CashSummary|null>(null);readonly registers=signal<any[]>([]);readonly loading=signal(false);registerId='';opening=0;counted=0;notes='';async ngOnInit(){await this.load();}async load(){this.loading.set(true);try{const[r,s]=await Promise.all([this.data.list<any>('cash_registers',{active:true}),this.cash.current()]);this.registers.set(r.items);this.session.set(s);this.summary.set(s?await this.cash.summary(s.id):null);if(!this.registerId&&r.items.length)this.registerId=r.items[0].id;}finally{this.loading.set(false);}}async open(){try{await this.cash.open(this.registerId,Number(this.opening));this.toast.show({title:'Caja abierta',variant:'success'});await this.load();}catch(e){this.fail(e);}}async close(){if(!this.session()||!confirm('¿Confirmás el cierre de caja con el efectivo contado indicado?'))return;try{await this.cash.close(this.session().id,Number(this.counted),this.notes);this.toast.show({title:'Caja cerrada correctamente',variant:'success'});await this.load();}catch(e){this.fail(e);}}private fail(e:unknown){this.toast.show({title:'No se pudo operar la caja',description:e instanceof Error?e.message:'Error inesperado',variant:'danger'});}}
