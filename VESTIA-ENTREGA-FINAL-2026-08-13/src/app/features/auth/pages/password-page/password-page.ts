import{Component,inject,signal}from'@angular/core';import{FormsModule}from'@angular/forms';import{ActivatedRoute,RouterLink}from'@angular/router';import{SupabaseService}from'../../../../core/services/supabase.service';@Component({selector:'app-password-page',imports:[FormsModule,RouterLink],templateUrl:'./password-page.html',styleUrl:'./password-page.css'})export class PasswordPageComponent{private readonly client=inject(SupabaseService).client;private readonly route=inject(ActivatedRoute);readonly sent=signal(false);readonly error=signal('');email='';password='';get updateMode(){return this.route.snapshot.data['mode']==='update';}async submit(){this.error.set('');if(this.updateMode){const{error}=await this.client.auth.updateUser({password:this.password});if(error)this.error.set(error.message);else this.sent.set(true);}else{const{error}=await this.client.auth.resetPasswordForEmail(this.email,{redirectTo:location.origin+'/nueva-clave'});if(error)this.error.set(error.message);else this.sent.set(true);}}}
