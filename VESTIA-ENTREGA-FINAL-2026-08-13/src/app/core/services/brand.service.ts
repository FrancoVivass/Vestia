import{Injectable,computed,inject,signal}from'@angular/core';import{Brand}from'../models/brand.model';import{SupabaseService}from'./supabase.service';@Injectable({providedIn:'root'})export class BrandService{private readonly client=inject(SupabaseService).client;private readonly state=signal<Brand[]>([]);readonly brands=computed(()=>this.state());constructor(){void this.load();}async load(){const{data,error}=await this.client.from('brands').select('*').order('name');if(!error)this.state.set((data??[]).map((x:any)=>({id:x.id,name:x.name,description:x.description??'',businessId:x.business_id,status:x.active?'active':'inactive',createdAt:x.created_at})));}async create(value:Omit<Brand,'id'|'createdAt'>){const{error}=await this.client.from('brands').insert({business_id:value.businessId,name:value.name,description:value.description,active:value.status==='active'});if(error)throw error;await this.load();}async update(id:string,value:Partial<Brand>){const{error}=await this.client.from('brands').update({name:value.name,description:value.description,active:value.status==='active'}).eq('id',id);if(error)throw error;await this.load();}async toggleStatus(id:string){const item=this.state().find(x=>x.id===id);if(item)await this.update(id,{status:item.status==='active'?'inactive':'active'});}}
